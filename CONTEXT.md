# CONTEXT

**Noobty** —— 局域网内的跨设备传输工具：以一台常驻服务器为中枢，以聊天 + 共享文件库的形式在设备间收发文字与文件；长期演进为局域网综合工具箱。

## 词汇表

- **中枢（Hub）**：局域网内常驻的 Ubuntu 服务器（局域网静态 IP，部署时以配置指定）。所有消息与文件经过它；它是服务端程序与网页 UI 的宿主。
- **设备（Device）/ 联系人**：接入中枢的终端，当前指多台 Windows PC 与安卓手机。设备即联系人，彼此**平等**：任何设备都可发送、下载、查看历史、删除。设备名认领**不区分大小写**。
- **消息（Message）**：聊天模型的基本单位，内容为一段文本或一个/一组文件（分享通知）。
- **文件库**：中枢上的共享文件区（NAS 形态）。聊天卡片与侧栏「文件」指向同一批字节；有保留天数与总容量上限，到期/超额自动清理最旧文件。
- **私聊**：以某台特定设备为目标的会话。对方在线时可选**直转**，离线时自动**入库**。
- **大厅（Lobby）**：所有设备共用的广播会话。发进大厅的内容任何设备可见、可下载。
- **入库（Store-and-Forward）**：传输模式之一。发送后文件即写入文件库，接收方可稍后下载。
- **直转（Streaming Relay）**：传输模式之二。收发双方同时在线，数据边上传边向接收方推送（仍落盘入库），不必等整文件传完再下。
- **下载**：本机从中枢文件库取回文件；本机记住「已下载」（跨刷新保留）。弱浏览器可走系统下载器直链，不必进页面堆。
- **发送中通知**：开始传文件时即向对端推送短暂「正在发送…」提示（不落库），对齐微信；真实消息在入库完成后到达。
- **网页 UI（多端）**：中枢提供的统一 Web 界面，是手机与客人设备的主要入口，也是 PC 托盘壳内嵌的界面。全项目只有这一套界面。
- **托盘壳（Shell）**：Windows 常驻托盘程序（Tauri 2），内嵌网页 UI，额外提供通知、自动接收、可配置接收目录。自身不维护独立界面。
- **压缩包场景**：首要痛点场景——以往用微信传压缩包，上行慢且有大小限制；Noobty 替代之，无大小限制、内网速度。
